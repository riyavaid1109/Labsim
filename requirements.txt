chromadb==1.5.9
ollama==0.6.2
matplotlib==3.10.9
numpy==2.4.6
rank-bm25==0.2.2

# Optional — cross-encoder reranking (~1.1GB model on first use)
# sentence-transformers>=3.0

# Optional — alternate LLM providers for the abstraction layer
# openai>=1.40
# anthropic>=0.34
